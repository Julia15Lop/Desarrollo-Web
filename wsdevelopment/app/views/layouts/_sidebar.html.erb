<%= javascript_include_tag "menu" %>
<%= stylesheet_link_tag "custom" %>

<% userId = user_id(session[:user_name])%>
<div class="left_col scroll-view" style="width: 100%">
	
  <!-- menu profile quick info -->
  <div class="profile clearfix" style="padding-top: 10px">
    <div class="profile_info">
      <p>Welcome, <%= session[:user_name] %></p>
    </div>
    <hr>
  </div>

  <!-- /menu profile quick info -->
  <!-- sidebar menu -->
  <div id="sidebar-menu" class="active main_menu_side hidden-print main_menu ">
    <div class="menu_section">
      <ul class="nav side-menu" id="nav-menu">
      	<% if user_session_is_admin? %>
	        <!-- User admin view -->
	        <li ><%= link_to '<i class="fa fa-sticky-note"></i> Notes list'.html_safe, user_notes_url(userId) %></li>
          <li ><%= link_to '<i class="fa fa-id-card-o"></i> Admin management'.html_safe, users_path %></li>
	        <!-- /User admin view -->
	    <% else %>
	    	<li ><%= link_to '<i class="fa fa-sticky-note"></i> Notes list'.html_safe, user_notes_url(userId) %></li>
	    <% end %>
        <!-- Collections view -->
        <li><%= link_to '<i class="fa fa-book"></i> Collections list'.html_safe, user_note_collections_url(userId) %>
        </li>
        <!-- /Collections view -->
        <!-- Friends view -->
        <li><%= link_to '<i class="fa fa-address-book"></i> Friends list'.html_safe, user_friendships_path(userId) %>
        </li>
        <!-- /Friends view -->  
        <!-- Edit view -->
        <li><%= link_to '<i class="fa fa-pencil"></i> Edit profile'.html_safe, edit_user_path(userId)  %>
        </li>
        <!-- /Edit view -->  
      </ul>
    </div>
  </div>
  <!-- /sidebar menu -->
</div>
